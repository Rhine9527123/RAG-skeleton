# RAG 财务知识库服务 + Hermes Agent - 部署指南

## 一、环境准备（只需安装 Docker）

### 1. 安装 Docker Desktop

- 下载地址：https://www.docker.com/products/docker-desktop/
- 安装后重启电脑
- 启动 Docker Desktop，等待左下角显示 "Engine running"（绿色状态）

> 如果你的电脑是 Windows 10，可能需要先开启 WSL2（Docker 安装时会自动提示）
> Windows 11 一般自带，不需要额外操作

### 2. 注册 Kimi 账号并获取 API Key

- 注册地址：https://platform.moonshot.cn/
- 进入控制台 → API Key 管理 → 创建新 Key
- 复制 Key（以 `sk-` 开头的字符串）

## 二、部署（3 步）

### 步骤 1：双击 `docker_start.bat`

首次运行会自动创建 `.env` 文件，并弹出记事本。

### 步骤 2：填写 API Key

在记事本中找到这行：

```
KIMI_API_KEY=在这里填写你的API_Key
```

把 `在这里填写你的API_Key` 替换成你从 Kimi 控制台复制的 Key，保存后关闭记事本。

### 步骤 3：再次双击 `docker_start.bat`

脚本会自动：
1. 检查 Docker 是否运行
2. 检查 API Key 是否已填写
3. 构建 RAG 服务镜像（首次约 5-10 分钟，后续几秒）
4. 拉取 Hermes Agent 镜像（首次约 2-5 分钟）
5. 启动所有服务

看到以下输出即表示部署成功：
```
部署完成！
  RAG 接口文档:   http://localhost:8000/docs
  Hermes 管理面板: http://localhost:3000
```

## 三、系统架构

```
┌─────────────────────────────────────────────────┐
│                    Docker 网络                    │
│                                                 │
│  ┌──────────────┐        ┌──────────────────┐   │
│  │  RAG 服务     │◄───────│  Hermes Agent     │   │
│  │  :8000       │  HTTP  │  :3000           │   │
│  │              │        │                  │   │
│  │ • FastAPI    │        │ • AI 对话界面      │   │
│  │ • 知识库检索  │        │ • 微信/飞书接入    │   │
│  │ • Reranker   │        │ • Skill 扩展      │   │
│  │ • 模型内置    │        │                  │   │
│  └──────┬───────┘        └────────┬─────────┘   │
│         │                         │             │
│    ┌────┴────┐               ┌────┴────┐        │
│    │ 知识库   │               │ 配置/记忆 │        │
│    │ data/   │               │hermes-data│       │
│    └─────────┘               └─────────┘        │
└─────────────────────────────────────────────────┘
```

- **RAG 服务**（8000 端口）：负责知识库检索和问答
- **Hermes Agent**（3000 端口）：负责 AI 对话、微信/飞书接入
- 两个服务通过 Docker 内网通信，用户无需配置网络

## 四、日常使用

| 操作 | 方法 |
|------|------|
| 访问 RAG 接口文档 | 浏览器打开 http://localhost:8000/docs |
| 访问 Hermes 管理面板 | 浏览器打开 http://localhost:3000 |
| 查看运行日志 | 双击 `docker_view_logs.bat` |
| 停止服务 | 双击 `docker_stop.bat` |
| 重新启动 | 双击 `docker_start.bat`（非首次不需要再填 Key） |
| 上传知识库文档 | 通过 http://localhost:8000/docs 页面的 `/upload` 接口 |

## 五、Hermes 配置说明

Hermes 首次启动后需要配置：

1. 浏览器打开 http://localhost:3000
2. 按照页面引导完成初始设置
3. 配置 LLM Provider（推荐 Kimi，和 RAG 用同一个 API Key）
4. 配置 MCP Server 连接 RAG 服务（地址：`http://rag-service:8000`）

详细 Hermes 配置请参考：https://github.com/soulter/hermes-agent

## 六、硬件要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 4 核 | 8 核及以上 |
| 内存 | 8 GB | 16 GB |
| 硬盘 | 15 GB 剩余空间 | 30 GB 剩余空间 |
| 网络 | 首次部署需联网 | 后续可离线（LLM 调用除外） |

## 七、常见问题

### Q: Docker Desktop 启动后报错 "WSL 2 installation is incomplete"
A: 打开 PowerShell（管理员），执行：
```powershell
wsl --install
```
安装完成后重启电脑。

### Q: 首次构建镜像很慢怎么办？
A: 镜像需要下载 Python 依赖和 2.2GB 模型文件，首次 5-10 分钟是正常的。后续构建会使用缓存，几秒钟就能完成。

### Q: 如何更换 API Key？
A: 直接用记事本编辑 `.env` 文件，修改 `KIMI_API_KEY` 的值，保存后双击 `docker_stop.bat` 停止服务，再双击 `docker_start.bat` 重启。

### Q: 数据存在哪里？重装系统会丢数据吗？
A: 知识库文档存在 `data/` 文件夹，向量索引存在 `chroma_data_server/` 文件夹，Hermes 配置存在 `hermes-data/` 文件夹。只要不删除这些文件夹，数据就不会丢失。

### Q: 如何完全卸载？
A:
1. 双击 `docker_stop.bat` 停止服务
2. 运行 `docker compose down -v` 删除 Docker 卷
3. 运行 `docker rmi rag-service` 删除 RAG 镜像
4. 删除项目文件夹即可
